// `server-only` 대체 (Issue #58).
//
// 이 패키지는 "이 모듈을 클라이언트 컴포넌트에서 import 하면 빌드가 깨지게 한다" 는
// 표식일 뿐이고, 값이나 동작을 갖지 않는다. 실제 구현은 plain node 에서 무조건
// throw 하므로(클라이언트 번들 조건이 아니면 에러를 던지는 것이 그 패키지의 전부다)
// node --test 에서는 빈 모듈로 바꾼다. production 코드의 판정은 아무것도 바뀌지 않는다.
export {};
